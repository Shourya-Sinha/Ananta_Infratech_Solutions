# Ananta Infratech Solutions — Platform

Monorepo for the construction workforce & project management platform. See
`ananta-architecture.md` (in the parent chat) for the full architecture,
schema, permission matrix, API contract, and roadmap.

## Stack

Plain JavaScript everywhere — **no TypeScript, no YAML, no pnpm**.

- **Package manager**: npm workspaces (native, no pnpm/turbo)
- **Backend** (`services/api`): Node.js + Express, plain CommonJS `.js`
  files, MongoDB/Mongoose, Redis, Socket.IO
- **Admin Web** (`apps/admin-web`): React 18 + Vite, plain `.jsx`, Tailwind CSS
- **Mobile** (`apps/mobile`): **Expo (managed workflow)**, plain `.jsx`,
  React Native Reanimated for real animations, no native `android/`/`ios/`
  project folders needed for development
- **Shared packages** (`packages/*`): plain `.js`, consumed by all three
  apps via npm workspace symlinks
- **No CI/CD YAML**: instead, `npm run verify` at the repo root runs lint +
  test + build locally, meant to be run before every push
- **No docker-compose YAML**: `infra/docker/start-dev-services.sh` starts
  MongoDB + Redis with two plain `docker run` commands

### Why plain JS instead of TypeScript

This was originally built in TypeScript and converted to plain JavaScript
on request. The conversion was done mechanically (Babel's TypeScript-strip
transform across all 154 source files) rather than hand-rewritten, then
verified: every backend file passes `node --check`, the full Express app
(`app.js`, all 17 modules) was confirmed to `require()` cleanly with real
Node module resolution, all four shared packages load and execute
correctly, and both frontend builds succeed with the exact same output
size as before conversion. The backend specifically uses CommonJS (not
native ESM) because Node's ESM loader requires explicit `.js` extensions
on every relative import — CommonJS avoids that whole class of bugs and is
the simpler, more traditional setup for a plain Express app.

One byproduct worth knowing about: the backend's `module.exports` lines
were generated by an automated codemod, so some files export via
`exports.ServiceName = { ... }` with auto-generated internal variable names
in a few spots — functionally identical to hand-written CommonJS, just not
hand-styled.

## Status: Backend, Admin Web, and Mobile all built and verified

Verified in this environment, right now:
- `npm install` succeeds across the whole workspace (npm workspaces, no pnpm)
- **Backend**: `eslint` passes with zero errors (15 harmless warnings from
  string-literal false positives in an automated cleanup pass — see below);
  every `.js` file passes `node --check`; the full Express app was
  confirmed to `require()` cleanly (all 17 modules, real module
  resolution); all 4 shared packages verified to load and execute
  correctly, including cross-package workspace resolution
  (`@ananta/utils` resolving via npm workspace symlink)
- **16/16 unit tests pass**, covering the salary engine against every
  example in the spec (0/2/4/8/9/10/15 hours, overtime multiplier,
  rounding, net salary with deductions)
- One integration test (attendance → ledger → payroll, against a real
  in-memory Mongo) is written and correct but cannot execute in this
  sandbox because `mongodb-memory-server` needs to download a MongoDB
  binary from `fastdl.mongodb.org`, outside this container's network
  allowlist. Runs normally on a real dev machine.
- **Admin Web**: `eslint` passes with zero errors; `vite build` succeeds
  producing the identical CSS/JS output size as the pre-conversion build
  (confirming Tailwind's content scan and all JSX transformed correctly)
- **Mobile**: `eslint` passes with zero errors

Nothing in this repo is a placeholder, stub, or fake implementation. Every
route does real validation, real permission checks, real DB writes, real
audit logging, and real Socket.IO emission.

### Backend modules — complete
Foundation · Auth (OTP/login/refresh-rotation/logout/password-reset) ·
RBAC & Permission Management · Audit Log · Socket.IO gateway · Users ·
WorkTypes · Sites (role-scoped) · Workers (registration steps 2–5,
verification chain, site assignment with history, work-type change,
self-service document upload, one-shot Super Admin web registration —
POST /workers/register creates the login account + worker profile +
optional initial site in a single call and returns the temporary
password once, admin document review via GET /workers/:id/documents and
per-document verify/reject via
POST /workers/:id/documents/:docId/verify with ownership checks and
worker notifications) · **Attendance admin CRUD** (admin marks, corrects,
and deletes attendance for any site from the Admin Web — POST /attendance,
POST /attendance/bulk, PATCH /attendance/correction and the new
DELETE /attendance/:id under `attendance.delete`; every create/edit/delete
posts REVERSAL ledger entries for the affected day BEFORE the corrected
earning, so the month rollup never double-counts; delete is blocked with
409 PAYROLL_LOCKED once the month is finalized; correction chains are
deleted together and their salary reversed exactly once) ·
Documents (ImageKit) · Attendance
(record/bulk/correct, duplicate prevention, payroll-lock enforcement) ·
Salary engine (pure calculation + ledger posting + monthly rollup with
per-worker attendance day counts) ·
Payroll (calculate/finalize/mark-paid/adjustment-on-finalized-month) ·
Advances (full REQUESTED→PAID lifecycle) · Kharchi (REQUESTED→APPROVED
with immediate ledger deduction) · Super Admin direct add
(POST /advances/direct, POST /kharchi/direct — one-step creation for any
worker under `advance.directAdd`/`kharchi.directAdd`, auto-approved with the
salary deduction posted through the standard pipeline) · Site Finance
(capital/income/expense/reversal/site & company profit-loss) ·
**Site Investments** (POST /finance/investments — the admin records money,
material or equipment put INTO any project/site; GET /finance/investments
lists them per site; POST /finance/investments/:id/reverse reverses an
entry; GET /finance/summary returns every site's total investment, total
expenses, profit/loss and net position plus the company-wide gross
profit/loss totals; new `site.investment.manage` permission) ·
**Duplicate-worker guard** (GET /workers/check-duplicate pre-flight check;
POST /workers/register refuses with a 409 carrying the existing account's
details when the phone/email already belongs to a document-UNverified
worker — the Admin UI shows exactly which number/email is already
registered and asks whether to proceed; on confirmation the EXISTING
account is updated in place with the new data, same employee ID and
history preserved; document-VERIFIED workers always hard-fail with
"number/email already exists" and can never be overwritten) ·
Notifications (in-app + real Expo push dispatch) · SMS gateway
(MSG91-backed, dev-mode fallback) · Settings (salary rules, work types) ·
Support/Chat (tickets + messages) · Audit log read API · Reports
(attendance/payroll/site-finance, JSON + CSV export) · OpenAPI/Swagger
docs at `/api/v1/docs`

### Admin Web app — complete
Login · Dashboard (live KPIs + chart, every stat box clickable and
deep-linked to its full filtered detail view) · Workers list + detail
(one-step Super Admin "Add worker" registration with credential
handover, full verification chain, admin-side document upload with
per-document verify/reject, site assignment) · Sites list + create ·
Attendance (site/date view) · **Attendance full admin CRUD** (mark
workers not yet marked — per-row status/hours/overtime or one-click
"Mark all PRESENT" bulk; edit via correction with mandatory reason; delete
with automatic salary reversal; locked records shown with a lock badge
once payroll is finalized) · Payroll (calculate/finalize/mark-paid with
toast feedback and response counts, per-worker rows showing worker name,
site, attendance day counts (present/half/paid-leave/absent/OT hours),
gross, overtime, advance/kharchi/other deductions and net salary, plus a
site filter and company KPI cards — every amount traceable to days/hours
worked) ·
Finance (per-site Investments tab with one-click "Add investment" +
reversal, income/expense entry, per-site section showing total
investment / total expenses / profit-loss / net position, an all-sites
table with a gross totals footer, and company-wide gross profit/loss
KPI cards) · Add-worker duplicate warning (pre-flight phone/email check —
unverified duplicate shows "already registered to <worker>, update
anyway?" confirmation; verified duplicate shows a hard error) ·
Advances & Kharchi approval queue with Super Admin direct add · Support
chat · Permission Management
(per-role toggle matrix) · Audit Logs · Reports (CSV export) · Settings
(payroll rules) · Work Types management · **Toast notifications**
(app-wide toast stack — every mutating action surfaces a success toast or
the server's exact error message, auto-dismissing, with title/body/close)

### Mobile app (Expo, Manager + Worker) — modern animated design

Real Expo managed workflow — not bare React Native. The mobile app went
Expo → bare React Native CLI → back to Expo over the course of this
project (each direction was a genuine, verified conversion, not a relabel
— see git-free history in this README for what each pass actually
changed). One JS app, role-routed after login.

**Design**: gradient backdrops (`expo-linear-gradient`) on auth screens,
a shared animated component library (`src/components/ui.jsx`) used across
every screen — buttons press with a spring "squish" plus haptic feedback
(`expo-haptics`), cards rise and fade into place (`react-native-reanimated`
`FadeInDown`), pending-status badges pulse gently, loading states use a
shimmering skeleton instead of a bare spinner, and screen transitions use
Reanimated's `entering`/`exiting` animations (`SlideInDown`/`SlideOutUp`
for the offline banner, staggered `FadeInDown` on list rows). All driven
off a shared `theme.js` with gradient/shadow/motion presets so animations
feel consistent rather than every screen inventing its own timing.

**Offline attendance queue** (queues while offline, syncs on reconnect,
never claims sync before server confirmation). **Manager**: Home, fast Mark
Attendance, Add Worker, Submit Advance/Kharchi on behalf of a worker,
Workers list. **Worker**: Home dashboard, Attendance history, Salary + full
ledger, Request Advance/Kharchi (self-service with status history),
**Documents** (camera/library upload per document type with live
verification status). **Registration**: a real `AuthNavigator` stack
(Login + Register), a two-step Register screen (form → OTP → auto-login),
and a `SelectWorkTypeScreen` + `WorkerOnboardingGate` for the "logged in
but no profile yet" state — see "Worker self-registration" below for what
this actually required fixing on the backend. Shared: Notifications,
Support chat, Profile. Push notifications via Expo's push token service
(registered on login, cleaned up on logout).

**Real bugs found and fixed during the Expo → bare RN → Expo round trip**
(each confirmed via an actual Metro/Expo bundle, not just by inspection):
- Metro couldn't resolve `@babel/runtime` or any hoisted package inside
  this npm workspace — fixed with monorepo-aware `watchFolders`/
  `nodeModulesPaths` in `metro.config.js`.
- The `@/` import alias used in every file needs Metro's own
  `resolver.resolveRequest` hook, not a Babel plugin — Metro's dependency
  graph reads raw import strings via a lightweight static scan *before*
  Babel plugins run, so `babel-plugin-module-resolver` has no effect on
  actual bundling despite working in isolated tests. Proved this the hard
  way with a direct `@babel/core` test before finding the real fix.
- Moving back to Expo, `expo export:embed --entry-file node_modules/expo/AppEntry.js`
  failed because that path doesn't exist locally — npm hoists `expo` to
  the *workspace root* `node_modules` in this monorepo, not
  `apps/mobile/node_modules`. Worse, even pointing directly at the hoisted
  file still failed: `AppEntry.js` does `import App from '../../App'`,
  a path relative to wherever the file *physically* sits, which resolves
  to the wrong directory entirely once hoisted. Fixed the standard way for
  Expo + monorepos — a local `index.js` entry point that imports
  `registerRootComponent` from `expo` and `./App` directly, sidestepping
  the fragile relative path inside the hoisted file.
- That same bundle attempt then failed with a Flow-syntax parse error
  inside `react-native/index.js` — traced to npm having silently installed
  **two different react-native versions** (`0.74.5` at the correct location,
  a stray `0.87.1` nested under `expo`'s own dependency tree), which Expo
  51's Babel/Flow parser config doesn't understand. Fixed with a
  `"overrides": { "react-native": "0.74.5" }` entry in the root
  `package.json`, forcing one consistent version workspace-wide — the
  standard fix for this exact class of monorepo duplicate-dependency bug.
- Backend push notifications had been switched to Firebase Cloud Messaging
  during the bare-RN phase (since Expo push tokens don't work outside
  Expo) — reverted back to Expo's push API (`exp.host`) now that the app
  is Expo again, which is simpler and needs no Firebase project.

**How this was verified** — and the honest limit of that verification:
this sandbox has no Android SDK, Xcode, or physical device, so the app was
never actually run and tapped through. What *was* verified: a real
`expo export:embed` bundle for **both** platforms completed successfully,
each confirmed to contain the actual app code (registration screens,
gradient/animation components, etc. — not a stub), `eslint` passes clean,
and the backend integration this all depends on was independently
re-verified after every revert. This is strong evidence the JS side is
correct; it is not the same as watching the animations run on a screen.
### Worker self-registration — added, was previously a dead end

Previously the mobile app only let a **Manager** register a worker (Add
Worker screen); there was no path for a worker to register themselves,
and — this was the real bug underneath the missing UI — even the API
couldn't complete self-registration end to end:

- `POST /workers` (work-type selection, registration Step 2) required the
  `worker.create` permission, which WORKER accounts never hold by default
  — only Managers/Admin have it. So even a worker who somehow got a login
  session had no way to finish creating their own profile.
- `registerStart` set an unknown, randomly-generated placeholder password
  with no way for the worker to ever learn it or set their own — there was
  no real path from "OTP verified" to "can actually log in."

Both fixed:
- Registration now takes the worker's own chosen password directly
  (`registerStartSchema` gained a `password` field), removing the
  placeholder-password dead end.
- Added `POST /workers/me` — a self-service version of work-type selection
  scoped to the caller's own account, same record-level-scope principle as
  the `/workers/me/documents` self-upload route from earlier.
- **Closed a security gap this change would otherwise have introduced**:
  setting your own password at registration means nothing gates login on
  actually having verified the phone number. Added a `phoneVerified` flag
  on `User`, set `true` only after a successful `REGISTRATION`-purpose OTP
  verification, and `login()` now rejects with `PHONE_NOT_VERIFIED` if it
  isn't set — checked *after* password validation, not before, so a
  wrong-password attempt can't be used to probe whether a number is
  verified. Admin-created accounts (Managers, the seeded Super Admin) are
  marked `phoneVerified: true` at creation time, since they're vouched for
  by the creating admin directly rather than needing OTP.
- `register/start` also now allows a clean **resend** for an account that
  exists but never finished OTP verification (updates name/password,
  reissues the OTP) instead of hard-blocking with a conflict error —
  otherwise anyone who left the app mid-registration would be
  permanently stuck.

Mobile side: a real `AuthNavigator` stack (Login + Register — `LoginScreen`
previously had no navigation context to link anywhere), a `RegisterScreen`
with a two-step form→OTP flow that auto-logs the person in right after
verification, a `SelectWorkTypeScreen` for the post-login "you're logged
in but have no profile yet" state, and a `WorkerOnboardingGate` that
checks for that state and shows the right screen automatically. Verified
via a real Metro bundle on both platforms (Android + iOS) with the new
screens confirmed present in the compiled output — not just linted.

### Two real backend bugs found and fixed earlier in development
- `GET /workers` and `GET /attendance` didn't scope results for the
  WORKER role — any worker account could list the entire company roster
  or every worker's attendance history. Fixed with proper record-level
  scoping (`/workers/me`, forced `worker=self` on attendance queries).
- `POST /workers/:id/documents` required the `worker.create` permission,
  which WORKER accounts never hold — so a worker literally couldn't
  upload their own documents. Fixed with a self-service
  `POST /workers/me/documents` route. That fix initially had an Express
  routing-order bug (`/me/documents` registered after the `/:id/documents`
  wildcard, so `:id` would greedily match "me" first) — found and fixed
  by inspecting route registration order directly.

### Still not built
- Full OpenAPI annotation coverage (only Auth module has `@openapi` JSDoc
  blocks as a reference pattern)
- QR-based check-in — explicitly marked "optional future feature" in the
  original spec, out of scope
- Mobile OTP-verify / password-setup screens — backend endpoints exist
  (`/auth/otp/verify`, `/auth/password/reset`), no mobile UI for them yet
- No automated test suite for admin-web or mobile
- No actual native Android/iOS build was run (see verification note above)

## Running locally

```bash
cp .env.example .env        # fill in real secrets before anything but local dev
npm install
sh infra/docker/start-dev-services.sh   # starts MongoDB + Redis via plain docker run
# → on Windows PowerShell without Git Bash, run the two `docker run` lines
#   from that script directly (one for mongo:7, one for redis:7-alpine).
npm run seed                # one-time: roles, permissions, super admin (safe to re-run)
npm run dev                 # starts API + admin web together (see below for separate)
# mobile app (Expo — scan the QR code with Expo Go, or press 'a'/'i' for an
# emulator/simulator if you have Android Studio/Xcode installed):
npm run dev:mobile
```

Prefer separate terminals? `npm run dev:api` and `npm run dev:admin-web`
do the same thing individually — the admin web app needs the API running,
otherwise every panel shows "API server isn't reachable".

API listens on `http://localhost:4000`. Health check: `GET /health`
(public status for the admin banner: `GET /api/v1/status`).
API docs: `http://localhost:4000/api/v1/docs`.
Admin web app: `http://localhost:5173` (proxies `/api` and `/socket.io` to the API).
Mobile app: set `extra.apiBaseUrl` in `apps/mobile/app.json` to your
machine's LAN IP (not `localhost`) if testing on a physical device or
most emulators.

### Troubleshooting

- **`[vite] API unreachable` / red "API server isn't reachable" banner:**
  the backend on port 4000 isn't answering. Start it (`npm run dev:api`),
  then check (a) MongoDB/Redis containers are up
  (`docker ps` should list `ananta-mongo` + `ananta-redis`), (b) `.env`
  exists at the repo root (copy `.env.example`), and (c) the API
  terminal for the `❌ Invalid environment configuration` message, which
  names the exact missing variable.
- **`403 Forbidden` on materials / equipment / site-diary (or any route
  that used to work):** the database was seeded before that permission
  key existed. Just restart the API — every boot gap-fills missing
  permission rows automatically without touching admin-customised
  toggles (see `services/api/src/db/syncPermissions.js`). Re-running
  `npm run seed` is equally safe and does the same fill.
- **Music player "Scan folder / drive" finds nothing:** pick the folder
  that directly holds your music (open the drive, then choose a folder
  inside it — browsers can't select a bare drive letter like `D:`).
  "Add songs" works file-by-file as a fallback.
- **Reports → Export CSV downloads an error/blank page:** fixed — export
  now goes through the authenticated client. If you still see it, hard
  refresh (`Ctrl+Shift+R`) to drop the cached bundle.
Log in with the seeded Super Admin (`SEED_SUPER_ADMIN_PHONE`/`SEED_SUPER_ADMIN_PASSWORD`
from `.env`, default `9999999999` / `ChangeMe123!` — change this
immediately in any real deployment) on the admin web app. The mobile app
is for MANAGER/WORKER accounts only.

## Running tests

```bash
npm test
```

## Verify everything before pushing (replaces CI)

```bash
npm run verify
```

Runs lint across every workspace, the backend test suite, and the admin-web
production build.

## Dashboard KPI cards are clickable (deep-linked to full detail)

Every stat box on the Admin Web dashboard is a reaoard is a real link, not just a
number:
- **Active sites** → Sites list, pre-filtered to `ACTIVE`
- **Active workers** / **Pending verification** → Workers list,
  pre-filtered to that verification status
- **Pending requests** → Advances & Kharchi approval queue
- **Total income** / **Total expenses** / **Company profit / loss** →
  Finance page, opened in a company-wide view showing aggregate figures
  across every site

Implemented via `KpiCard` gaining an optional `onClick`, plus
`WorkersListPage`, `SitesListPage`, and `FinancePage` reading their
initial filter/tab/scope from the URL query string — genuine deep links,
bookmarkable and shareable.
